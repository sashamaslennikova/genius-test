<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Смішне питання</title>

    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f7f7f7;
            font-family: Arial, sans-serif;
        }

        .box {
            text-align: center;
        }

        .question {
            font-size: 32px;
            margin-bottom: 20px;
        }

        .btn {
            padding: 15px 25px;
            margin: 10px;
            font-size: 22px;
            cursor: pointer;
            border: none;
            border-radius: 10px;
        }

        #yes {
            background-color: #4CAF50;
            color: white;
        }

        #no {
            background-color: #E74C3C;
            color: white;
            position: absolute;
        }

        .answer {
            font-size: 40px;
            margin-top: 25px;
            display: none;
        }
    </style>
</head>

<body>
    <div class="box">
        <div class="question">Сосав?</div>
        <button id="yes" class="btn">Да</button>
        <button id="no" class="btn">Нет</button>
        <div class="answer">Так і знав 😏</div>
    </div>

    <script>
        const noBtn = document.getElementById("no");
        const yesBtn = document.getElementById("yes");
        const answer = document.querySelector(".answer");

        noBtn.addEventListener("mouseover", () => {
            let x = Math.random() * window.innerWidth * 0.7;
            let y = Math.random() * window.innerHeight * 0.7;
            noBtn.style.left = x + "px";
            noBtn.style.top = y + "px";
        });

        yesBtn.addEventListener("click", () => {
            answer.style.display = "block";
        });
    </script>
</body>
</html>
